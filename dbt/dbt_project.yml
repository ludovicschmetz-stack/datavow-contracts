name: 'datavow_contracts_demo'
version: '1.0.0'
config-version: 2

profile: 'datavow_demo'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

vars:
  datavow_fail_on: broken
